schema = 1
owner = "runtime-tokio"
mode = "concurrent-independent"
rule-set = "phase-1a-independent-v1"
claims = ["boundary-config", "different-config", "identical-config", "independent-real-resources", "two-app-in-process"]
tests = ["rust_agent_runtime_tokio::tests::every_bundle_owns_an_independent_runtime"]
