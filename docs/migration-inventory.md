# AINS migration inventory

This inventory records behavioral inputs inspected from AINS and the rust-agent
capability boundary that replaces them. No rust-agent test or package depends on
the checked-out AINS tree.

| Phase | AINS input | rust-agent owner | Migrated behavior | Status |
|---|---|---|---|---|
| 3.1 | `AINS/crates/rust-agent/src/tools/mod.rs` | `rust-agent-tools` | Object-safe Tool contract, immutable definition capture, opaque handler registration | Complete |
| 3.1 | `AINS/crates/rust-agent/src/tools/outputs.rs` | `rust-agent-tools` | Inline output item/byte/depth admission before retention; UTF-8-safe error bounds | Complete |
| 3.4 | `AINS/crates/rust-agent/src/tools/runtime.rs` | `rust-agent-runtime-api`, `rust-agent-agent`, `rust-agent-tools` and `tool-executor-guarded` | Rewritten as sealed provider snapshots, a private bounded registry, pure model-origin planning and an exact ToolCall-proof-gated permission/approval/permit/provider path; the legacy public ToolRuntime/handler map is not copied. Generated scope assembly records the exact selected driver → ToolExecutor edge, retains the issuer only in AgentContext and gives the scope factory only an opaque provider-bound verifier envelope | Complete |
| 3.5 | New architecture boundary | `rust-agent-tools` and `tool-executor-guarded` | Bounded typed pre/around/post/observer middleware with immutable guarded context, deterministic order, reverse exit, stage-exact failure policy, read-only cancellation state, fixed deadline lineage and observer failure containment; no universal string hook bus or output fabrication path is introduced | Complete |
| 3.6 | `AINS/crates/rust-agent/src/tools/runtime.rs` composite-tool behavior | `rust-agent-tools` | Nested tools receive a non-cloneable invariant-lifetime borrowed session from the exact current permit/context pair; calls retain the sealed registry and root lineage, re-enter schema/policy/approval/middleware/output guards, tighten effects, and reject cancellation, depth/count/cost/cycle or closed-owner violations before raw provider dispatch. Tool call cost is a bounded, build-reviewed part of sealed registration identity, and Nested/Command origins charge one shared aggregate budget before permission/provider callbacks | Complete |
| 3.7 | New Command-to-Tool authority boundary | `rust-agent-commands` and `rust-agent-tools` | An exact Command permit/context pair derives a non-cloneable invariant-lifetime Tool grant with bounded call/output/effect authority. Tool preparation validates Agent/lifecycle, executor identity and the exact clock/sleep projection, then enters the same guarded dispatch; the complete non-empty CommandDispatcher and its journal-backed issuance remain assigned to later phases | Partial |
| 3.8 | `AINS/crates/rust-agent/src/tools/runtime.rs` scheduling behavior | `rust-agent-tools` | Model-origin prepared calls execute through a rolling scheduler with a checked hard concurrency cap, global-exclusive and derived same-resource barriers, exact batch lineage preflight, cancellation/deadline dispatch stop, a guarded terminal outcome for every admitted call, and model-order outcomes; no executor, thread pool or raw handler surface is added | Complete |
| 3.11 | New architecture boundary | `rust-agent-model` | Provider-neutral Tool-call stream events use a private-field checked DTO with bounded canonical name, arguments, provider call id, aggregate count and output bytes; the model API does not import Tool implementations | Complete |
| 3.12 | `AINS/crates/rust-agent/src/agent/tool_loop.rs` behavior | `driver-tools` | Rewritten as an API-only bounded state machine that fixes each Tool snapshot before the model request, derives stable normalized call ids, rejects malformed/duplicate calls before effects, journals and seals every plan, dispatches through the guarded bounded scheduler, preserves model order and caps steps/history/usage; no legacy kernel or raw Tool registry is copied | Complete |
| 3.13 | New generated composition boundary | `rust-agent-composition` | Generated Agent assembly resolves and constructs permission-default → tool-executor-guarded → driver-tools, claims the exact driver-to-executor consumer edge, projects the union of required runtime primitives, builds and executes an isolated locked composition, and proves Tool packages are absent from the minimal graph and lockfile | Complete |
| 3.1 | `AINS/crates/rust-agent/tests/tool_runtime.rs` | `rust-agent-tools` unit and compile-fail suites | Registration, output budget and private-boundary regression shape without legacy `AgentKernel`/`ToolRuntime` imports | Complete |
| 3.2 | `AINS/crates/rust-agent/src/policy/permission_engine.rs` | `rust-agent-policy` and `permission-default` | Three-state allow/ask/deny seam; the default provider allows read-only actions and requires approval for higher-risk actions without importing mutable AINS mode/path state | Complete |
| 3.3 | `AINS/crates/rust-agent/src/context/prompt_pipeline.rs` | `rust-agent-prompt` | Deterministic contributor order is replaced by an identity-bound, transactional, bounded contributor contract; concrete prompt sections remain Phase 6 Components | Partial |
| 3.3 | `AINS/crates/rust-agent/src/context/compact.rs` | `rust-agent-prompt` | Conversation compaction, tool-result pruning and token metering are split into bounded capability seams; AINS algorithms and hidden model coupling are not ported in Phase 3 | Partial |
| 3.3 | `AINS/crates/rust-agent/src/perception/mod.rs` | `rust-agent-attachments` | Attachment bytes/media identity become a content-digested, range/budget/cancellation-checked storage contract; perception providers remain out of scope | Partial |
| 3.3 | New architecture boundary | `rust-agent-spill` | Agent-owned ephemeral spill references, expiry, bounded range access and owner teardown are separate from durable Attachments | Complete |
| 3.3 | New architecture boundary | `rust-agent-telemetry` | Closed structured event/attribute vocabulary prevents arbitrary secret-bearing strings and keeps sinks opaque | Complete |
| 4.1 | New architecture boundary preceding the AINS filesystem port | `rust-agent-fs` | Provider-neutral canonical `AgentPath`, bounded read/range/directory/write DTOs, stable provider-bound pagination and consumer bindings that reject cancellation, budgets and invalid provider output before any concrete local filesystem implementation is introduced | Complete |
| 4.2 | New architecture boundary preceding local filesystem providers | `rust-agent-resource-namespace`, `resource-namespace-bootstrap-local` | Exact projected bootstrap route, private one-shot preparation context, schema-owned namespace commitment and descriptor/config pairing; Linux opens the relative root with no-symlink `openat2`, binds device/inode identity and retains the directory descriptor across path replacement | Complete |
| 4.3 | `AINS/crates/rust-agent/src/tools/filesystem.rs` | `fs-read-local`, `fs-local`, `tool-fs` | Rewritten around retained descriptor anchors, `openat2` containment, hard-link rejection, stable mutation-bound cursors, bounded writes and provider-only `.gitignore`-aware search; no legacy kernel, application context or raw path-root import is retained | Complete |
| 4.4 | New architecture boundary preceding the AINS sandbox/shell port | `rust-agent-policy::process`, `rust-agent-process` | Separates raw process intent, sandbox confinement, subprocess mechanics, shell and terminal contracts. An Agent-scoped issuer/verifier pair seals ceiling-projected closed backend plans into non-cloneable confined specs, and subprocess handles are accepted only with post-setup enforcement evidence | Complete |
| 4.5 | AINS sandbox policy projection behavior | `sandbox-linux` | Reduced to the issuer-only, effect-free planning seam: requests are intersected with the immutable Agent ceiling, checked against the closed Linux primitive schema and sealed to the exact authority; no process execution or fabricated enforcement report is retained | Complete |
| 4.6 | AINS local subprocess mechanics and Linux sandbox launcher behavior | `subprocess-local` | Rewritten as a verifier-only two-stage provider with an I/O-free factory and same-owner typed initialize/shutdown hooks. Initialization digest-pins backend/launcher, workspace, runtime and executable descriptors for a fresh bubblewrap namespace; the launcher applies resource limits, Landlock, no-new-privileges and x32-safe seccomp, confirms target exec, then emits its fixed setup acknowledgement. Shared bounded output, serialized idempotent process-group cleanup, first-cause cancellation/deadline/output classification, setup-failure reap and lifecycle teardown replace legacy sandbox/executor coupling | Complete |
